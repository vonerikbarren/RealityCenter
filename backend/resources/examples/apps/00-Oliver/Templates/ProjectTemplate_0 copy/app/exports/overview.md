# Overview for Git export

`github repo` goes in this [mkdir:exports] because this is where it will be exported. 

the github repo will always be a folder, so it will always be first before this file!