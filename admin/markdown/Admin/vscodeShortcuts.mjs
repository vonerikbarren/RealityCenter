class VsCode_Tree_Learn {
    constructor() {
        this.admin = []
        this.storage = []

        this.keyboardShortcuts = function keyboard_shortcuts (

        ) {
            let cmd = "command"
            let alt = "alt"
            let ctrl = "control"
            let key = function (input) {
                return `Key + ${input}`
            }

            // -------------------------------------
            let wordWrap = cmd + key(z)
        }
    }
}