export default function OmniData
(
    groupInit, 
    headerData,
    hiearchyPosition, 
    numOfContext, arrOfAssets, arrOfObjects, 
    px, py, pz,
    rx, ry, rz,
    sx, sy, sz,
    adminData,
) 

{
    let admin = [adminData]
    let storage = []

    let headerData = headerData

    let groupInit = groupInit
    let hiearchyPosition = hiearchyPosition
    let numOfContext = numOfContext
    let arrOfAssets = arrOfAssets
    let arrOfObjects = arrOfObjects

    let px = px
    let py = py
    let pz = pz

    let rx = rx
    let ry = ry
    let rz = rz

    let sx = sx 
    let sy = sy
    let sz = sz
}